apiVersion: v1
kind: Service
metadata:
  name: {{ include "mav1.fullname" . }}
  labels:
    {{- include "mav1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mav1.selectorLabels" . | nindent 4 }}
